# plans/ Directory — READ THIS FIRST

The files `v2-*.md` through `v9-*.md` in this directory are **archived iterations** of the parameter optimization plan. They are SUPERSEDED and kept only for historical reference.

The current authoritative plan is:

    plans/development/baseline-plan.md

Do NOT use any `v*.md` file in this directory for implementation decisions. Always read `plans/development/baseline-plan.md` instead.
