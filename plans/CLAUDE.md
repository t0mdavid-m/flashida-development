# plans/ Directory — READ THIS FIRST

The files `v2-*.md` through `v9-*.md` in this directory are **archived iterations** of the parameter optimization plan. They are SUPERSEDED and kept only for historical reference.

The current working plans are the per-phase implementation plans:

    plans/development/Phase_0/ through Phase_8/implementation-plan.md

These are the ONLY files that should be edited during implementation. The files `baseline-plan.md`, `testing-strategy.md`, `implementation-roadmap.md`, and `verification-report.md` in `plans/development/` are read-only reference documents.
